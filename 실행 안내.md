# 이 컴퓨터에서 포트폴리오 실행하기

이 폴더는 소스, 이미지, 설정, 설치된 패키지를 포함한 독립된 사이트 프로젝트입니다.

## 수정하면서 실행
1. `사이트 실행.cmd`를 더블클릭합니다.
2. 창에 Ready가 표시되면 브라우저에서 http://localhost:3000 을 엽니다.
3. 실행 창은 켜 둡니다. 종료하려면 창에서 Ctrl+C를 누릅니다.
4. 이 폴더의 src 파일을 수정하고 저장하면 화면에 자동 반영됩니다.

## 완성본 실행
개발 서버를 먼저 종료한 뒤 `배포용 실행.cmd`를 더블클릭합니다. 빌드를 마치면 같은 주소로 완성본 서버가 실행됩니다.

## 주요 수정 파일
- `src/app/globals.css`: 하단 메뉴, 색상, 크기, 간격
- `src/components/HorizontalStream.tsx`: 메인 페이지와 메뉴
- `src/lib/projects.ts`: 프로젝트 내용
- `public/images`: 사이트 이미지

## 직접 명령어로 실행
이 폴더에서 터미널을 열고 `npm.cmd run dev`를 실행합니다.
타입 검사: `npm.cmd run typecheck`
완성본 빌드: `npm.cmd run build`
완성본 서버: `npm.cmd start`

3000 포트가 이미 사용 중이면 기존 사이트 실행 창을 종료하거나 `npm.cmd run dev -- --port 3001`을 실행한 뒤 http://localhost:3001 로 접속합니다.

같은 Wi-Fi의 다른 기기에서는 이 PC의 IPv4 주소와 :3000으로 접속할 수 있습니다. 이때 Windows 방화벽의 개인 네트워크 허용이 필요할 수 있습니다. localhost 주소는 이 컴퓨터용이며 인터넷 공개 주소는 아닙니다. 외부 공개에는 별도의 배포 또는 네트워크 설정이 필요합니다.

다른 컴퓨터로 옮길 때는 Node.js LTS를 설치하고 이 폴더를 복사하면 됩니다. node_modules와 .next는 복사하지 않아도 되며 실행 파일이 필요한 패키지를 설치합니다.
